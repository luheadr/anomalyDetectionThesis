#rebuild the image
docker build -t pytorch_anomaly_detection .

#run the image
docker run --rm -v /home/pi/anomalyDetectionThesis:/app pytorch_anomaly_detection



git:
git add .
git commit -m "your message"
git push